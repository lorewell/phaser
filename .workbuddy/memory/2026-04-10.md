# 2026-04-10

## Phaser 擂台对战游戏初始化

- 创建了 `index.html` + `game.js`，使用 Phaser 3.60.0（CDN）
- 游戏内容：600×600 画布，440×440 擂台（金色边框），两个 50×50 玩家（蓝色=P1，红色=P2）
- 玩家开局随机方向 220px/s 直线移动，碰擂台边界反弹，玩家间 AABB 碰撞后速度分量交换
- 碰撞有粒子闪光特效 + 镜头轻微震动
- 本地服务器：`python -m http.server 3000 --directory d:/lw/phaser`，访问 http://localhost:3000
- Phaser CDN 国内不稳定，已下载本地 `phaser.min.js`，index.html 改为本地引用

## 下午更新

- 去掉粒子特效、擂台网格装饰、速度调试文本
- 背景改为白色，擂台内部白色填充 + 黑色边框
- 新增血量系统：`hp1 / hp2` 初始均为 100，底部显示双侧血条（蓝/红）+ `当前/最大` 数字标注
- 画布高度从 600 调整为 640 以留出血条空间

## 下午更新 - 角色系统

- 新增 `characters.js` 角色配置文件，包含两个角色：
  - **疾风 (swift)**：蓝色，速度 320px/s，血量 100，尺寸 50px
  - **铁壁 (tank)**：红色，速度 180px/s，血量 150，尺寸 55px（稍大）
- `game.js` 重构为读取角色配置，支持不同速度、血量、尺寸
- 血条根据各角色 `maxHp` 动态计算比例
- `DEFAULT_PLAYER1_CHAR / DEFAULT_PLAYER2_CHAR` 控制默认角色分配
- `index.html` 引入 `characters.js` 脚本

## 下午更新 - 4角色+进攻手段系统

- 角色扩展到4个，属性平衡设计（速度+血量=420）：
  | 角色 | 血量 | 速度 | 定位 | 进攻手段 |
  |------|------|------|------|----------|
  | **疾风** | 90 | 330 | 高速游击 | 疾风连击（近战，高频低伤，连击叠加） |
  | **铁壁** | 180 | 140 | 肉盾坦克 | 钢铁反震（光环，被动25%反伤+范围伤害） |
  | **影刃** | 110 | 280 | 刺客突袭 | 暗影冲锋（冲锋，2.5倍速撞击高伤） |
  | **神射** | 120 | 200 | 远程狙击 | 精准射击（远程，距离越远伤害越高） |

- 攻击类型枚举：`MELEE`/`RANGED`/`AURA`/`CHARGE`
- 新增视觉效果：伤害数字飘字（红色）、反伤标记（橙色↻）、受伤闪烁白、射击绿线
- 画布高度调整为 680 以容纳更多信息
